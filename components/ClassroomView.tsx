import React, { FC } from 'react';

/**
 * This is an obsolete component that is no longer used by the application.
 * It is being kept as a valid, empty component to prevent a build error
 * that occurs when this file is empty. It renders nothing.
 */
export const ClassroomView: FC = () => {
    return null;
};
